# 输入目录约定

把你的原始 Word/RTF 数据文件放到本目录：

- 文件后缀：`.doc / .docx / .rtf`
- 建议文件名包含 `part1 / part2 / part3`（如有），脚本会按 part 顺序优先匹配；不含也可以。
- 不要放微软/ WPS 打开的临时锁文件：`~$*.rtf / ~$*.docx`（脚本会自动跳过这些锁文件，但尽量保持干净）。

脚本会遍历本目录下所有 Word/RTF 并自动生成 Excel 输出（保留图表/透视结构，写入阶段使用 Excel COM，避免 Office 修复弹窗）。

