# Clinical Data Automation — Python 依赖（pip install -r requirements.txt）
#
# 安装说明
# - 全量安装体积较大（含 PaddleOCR / paddlepaddle）；若不需要 11_PPTX_PDF_to_PPT，可暂时注释对应行。
# - 非 Windows 或不用 Office COM 自动化时，可移除或注释 pywin32 环境标记行后再安装。
# - 各编号目录为独立工具；未使用的模块不会强制加载，但依赖会一并装上。

# ---------------------------------------------------------------------------
# 核心表格与数据处理（多模块共用）
# ---------------------------------------------------------------------------
openpyxl>=3.1.0
pandas>=1.5.0
xlsxwriter>=3.0.0
numpy>=1.20.0

# ---------------------------------------------------------------------------
# PDF：解析、渲染、合并、幻灯片（03/04/07/16/17/19/23 等）
# ---------------------------------------------------------------------------
pdfplumber>=0.10.0
pymupdf>=1.23.0
pypdf>=4.0.0
python-pptx>=0.6.21
Pillow>=10.0.0

# ---------------------------------------------------------------------------
# 配置（03_PDF_to_Excel 等）
# ---------------------------------------------------------------------------
pyyaml>=6.0

# ---------------------------------------------------------------------------
# 机器学习 / 统计（02_PPT_Merge TF-IDF）
# ---------------------------------------------------------------------------
scikit-learn>=1.0.0

# ---------------------------------------------------------------------------
# Windows Office 自动化（COM：09/20/24/08 等；仅 Windows 安装 pywin32）
# 20_Word_to_Excel_to_Figure：Word 读表 + Excel 写回保结构，依赖 openpyxl 与 pywin32
# 24_Word_Tables_to_Excel：Word 指定表格抽取导出 xlsx，依赖 openpyxl 与 pywin32
# ---------------------------------------------------------------------------
pywin32>=306; platform_system == "Windows"

# ---------------------------------------------------------------------------
# OCR 与深度学习（11_PPTX_PDF_to_PPT；可选，注释可跳过）
# ---------------------------------------------------------------------------
paddleocr>=2.6.0
paddlepaddle>=2.5.0
tqdm>=4.66.0

# ---------------------------------------------------------------------------
# OCR 可选后端（16_PDF_Sanitizer 标题提取后备）
# ---------------------------------------------------------------------------
pytesseract>=0.3.10

# ---------------------------------------------------------------------------
# 网络与翻译（15/21/22）
# ---------------------------------------------------------------------------
requests>=2.31.0
translators>=5.9.2
python-docx>=1.1.0

# ---------------------------------------------------------------------------
# 打包（12_Py_to_EXE）
# ---------------------------------------------------------------------------
pyinstaller>=5.13.0

# ---------------------------------------------------------------------------
# 以下模块仅标准库，无额外 pip 包：10_C_Drive_Cleanup、13_WiFi_Passwords、
# 14_Folder_File_Count（复用 pandas/openpyxl）、18_Proxy_Config_Export
# ---------------------------------------------------------------------------
